======================================================================
  ADSLM — ADAPTIVE DATA SCIENCE LANGUAGE MODEL
  Enterprise AI Evaluation Report
  Generated: 2026-09-02 12:45:31
======================================================================

── PIPELINE OVERVIEW ─────────────────────────────────────────────
  Task Type    : Regression
  Target Column: industry_id
  Best Model   : XGBoost Regressor
  Best Score   : 597.1024

── DATASET INSIGHT ───────────────────────────────────────────────
  The dataset contains 164,808 records across 2 features (2 numerical, 0 categorical). The dataset is complete — no missing values detected. Outliers were detected in 1 column(s) using the IQR method — these were retained for training but may warrant domain investigation. | Pipeline: median imputation for continuous features (resistant to skew), mode imputation for categoricals. IQR-based outlier flagging applied. One-Hot Encoding via pd.get_dummies (drop_first=True to avoid multicollinearity).

── TASK DETECTION ────────────────────────────────────────────────
  A Regression problem was detected because the target column 'industry_id' holds continuous numerical values. The model will predict quantities — such as energy consumption, machine temperature, or throughput rate. | Stratified 80/20 train-test split applied (stratify=y for classification). No data leakage — preprocessing fit exclusively on training fold.

── PREPROCESSING SUMMARY ─────────────────────────────────────────
  Numerical missing values filled using median imputation. All features standardised to zero mean and unit variance (StandardScaler). Final feature matrix contains 1 columns after encoding. | StandardScaler fitted on X_train only; transform applied to both train/test to prevent test-set leakage. Boolean dummy columns cast to int8 for memory efficiency.

── MODEL RECOMMENDATIONS ─────────────────────────────────────────
  [1] Linear Regression
      → Serves as a transparent, highly-interpretable baseline. Ideal for datasets with approximately linear feature-target relationships. Widely used in industrial applications for process parameter prediction.
  [2] Random Forest Regressor
      → Ensemble of decision trees that captures non-linear interactions and feature dependencies. Robust to outliers and handles mixed feature types well — excellent for complex sensor data regression.
  [3] XGBoost Regressor
      → Gradient-boosted tree model; state-of-the-art on structured tabular data. Scales efficiently with large datasets and includes built-in regularization to prevent overfitting on noisy industrial data.

── PERFORMANCE METRICS ───────────────────────────────────────────
  RMSE                     : 597.1024
  MAE                      : 267.1921
  R2                       : 0.0167

── TOP FEATURE IMPORTANCES ───────────────────────────────────────
   1. job_id                         1.0000  ████████████████████████████████████████

── AI MODEL INSIGHT ──────────────────────────────────────────────
  After training and comparing all recommended models, XGBoost Regressor achieved the best performance. It minimised the Root Mean Squared Error (RMSE) to 597.1024 with an R² of 0.0167, explaining 1.7% of the variance in the target variable. | Model selection criterion: weighted F1 (classification) or RMSE (regression). All models trained with default hyperparameters; recommend GridSearchCV / Optuna for production hyperparameter optimisation.

── FEATURE INSIGHT ───────────────────────────────────────────────
  The top predictive features are 'job_id'. These variables carry the highest statistical weight in the model's decision process. In an industrial context, focusing data quality efforts on these columns will yield the greatest improvement in prediction accuracy. | Importances sourced from estimator's native feature_importances_ (tree-based) or mean(|coef_|) (linear). For model-agnostic explanations, integrate SHAP TreeExplainer / KernelExplainer.

── BIG DATA TELEMETRY & SCALE ────────────────────────────────────
  Throughput Speed : 48.5 MB/s (164,808 records)
  PySpark RAM Req. : 512.0 MB (2 executors)
  Partition Column : 'job_id' (4 partitions)
  Storage Format   : Delta Lake / Apache Parquet (ACID compliant)

── REGULATORY & AI GOVERNANCE AUDIT ──────────────────────────────
  Compliance Rating: 100 / 100 (ISO 27001 Aligned)
  EU AI Act Risk   : Medium Risk (General Industrial Energy & Process Optimization)
  GDPR Privacy     : Clean — No raw PII stored
  Human Oversight  : Mandatory (Human operator must approve critical fault actions)
      → Article 52: Inform operator of AI-generated continuous predictions.
      → Maintain audit logs of training dataset parameters and metrics.

── ACTIONABLE RECOMMENDATIONS ────────────────────────────────────
  1. Focus sensor calibration and quality assurance on top predictive features: 'job_id'.
  2. Investigate extreme sensor spikes detected in columns ['job_id', 'industry_id'] via IQR bounds.
  3. R² (0.0167) is low; consider engineering non-linear feature interactions.
  4. Partition Delta Lake tables on column 'job_id' for sub-second distributed telemetry queries.
  5. Deploy the saved model binary via the FastAPI /orchestrate endpoint for production inference.

======================================================================
  End of ADSLM Report | Enterprise AI Evaluation
======================================================================